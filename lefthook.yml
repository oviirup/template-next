pre-commit:
  jobs:
    - name: format staged files
      run: biome format --write {staged_files}
      glob: "**/*.{js,jsx,ts,tsx,json,jsonc,css}"
post-checkout:
  jobs:
    - name: update submodules after checkout
      run: git submodule update --init --recursive
